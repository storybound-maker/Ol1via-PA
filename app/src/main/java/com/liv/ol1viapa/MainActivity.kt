package com.liv.ol1viapa

import android.content.Intent
import android.provider.Settings

// NOTE: The settings hub action must launch LeauSettingsActivity, not Android Settings.
// The existing MainActivity implementation should use:
// context.startActivity(Intent(context, LeauSettingsActivity::class.java))
